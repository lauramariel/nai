global:
  imagePullSecrets:
    - name: ${IMAGE_PULL_SECRET}
  storage:
    storageClassName: ${NAI_DEFAULT_RWO_STORAGECLASS}
    storageClassNameRWX: ${NAI_API_RWX_STORAGECLASS}

gateway:
  envoyDeployment:
    container:
      image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-envoy:distroless-v1.38.0

naiIepOperator:
  iepOperatorImage:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-iep-operator

  modelProcessorImage:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-python-processor

  dataSourceProcessorImage:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-python-processor

  batchInferenceProcessor:
    containers:
      processor:
        image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-go-processor
      statusProvider:
        image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-go-processor

  finetuneProcessor:
    containers:
      processor:
        image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-finetuning
      statusProvider:
        image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-go-processor

naiInferenceUi:
  naiUiImage:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-inference-ui

naiJobs:
  naiJobsImage:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-jobs

naiApi:
  naiApiImage:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-api
  supportedTGIImage: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-tgi
  supportedKserveRuntimeImage: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-kserve-huggingfaceserver
  eppImage: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-epp-inference-scheduler
  supportedVLLMImage: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-vllm
  supportedKserveCustomModelServerRuntimeImage: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-kserve-custom-model-server

naiDatabase:
  clientImage: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-postgresql:17.10-standard-trixie

naiIam:
  iamProxy:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-iam-proxy

  iamProxyControlPlane:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-iam-proxy-control-plane

  iamUi:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-iam-ui

  iamUserAuthn:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-iam-user-authn

  iamThemis:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-iam-themis

  iamThemisBootstrap:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-iam-bootstrap

naiAgent:
  agentImage:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-agent-app

naiLabs:
  labsImage:
    image: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-rag-app

nai-clickhouse-keeper:
  clickhouseKeeper:
    image:
      registry: ${IMAGE_REGISTRY_URL}
      repository: ${PROJECT}/nai-clickhouse-keeper

oauth2-proxy:
  image:
    repository: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-oauth2-proxy

nai-clickhouse-server:
  clickhouse:
    image:
      registry: ${IMAGE_REGISTRY_URL}
      repository: ${PROJECT}/nai-clickhouse-server
    initContainers:
      addUdf:
        image:
          registry: ${IMAGE_REGISTRY_URL}
          repository: ${PROJECT}/nai-clickhouse-udf
      waitForKeeper:
        image:
          registry: ${IMAGE_REGISTRY_URL}
          repository: ${PROJECT}/nai-jobs

nai-clickhouse-schemas:
  image:
    registry: ${IMAGE_REGISTRY_URL}
    repository: ${PROJECT}/nai-clickhouse-schemas

naiMonitoring:
  opentelemetry:
    collectorImage: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-opentelemetry-collector-contrib:0.152.0
    targetAllocator:
      image:
        repository: ${IMAGE_REGISTRY_URL}/${PROJECT}/nai-target-allocator
  nodeExporter:
    serviceMonitor:
      namespaceSelector:
        matchNames:
          - prometheus
          - kommander
          - kommander-default-workspace
          - ${NKP_WORKSPACE_NAMESPACE}
  dcgmExporter:
    serviceMonitor:
      namespaceSelector:
        matchNames:
          - prometheus
          - kommander
          - kommander-default-workspace
          - ${NKP_WORKSPACE_NAMESPACE}
